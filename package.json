{
  "name": "gpn-be",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prestart": "npm run migration:deploy",
    "start": "node build/src/app/main.js",
    "start:dev": "ts-node-dev --respawn --transpile-only --exit-child --watch src src/app/main",
    "tsc": "tsc",
    "clean": "rm -rf dist",
    "build": "run-s clean generate:prisma tsc",
    "prisma:studio": "prisma studio",
    "migration:dev": "prisma migrate dev",
    "migration:create": "prisma migrate dev --create-only",
    "migration:status": "prisma migrate status",
    "migration:deploy": "prisma migrate deploy",
    "generate:prisma": "prisma generate",
    "postinstall": "run-s generate:prisma",
    "compile": "tsc",
    "playground": "graphql playground",
    "prisma": "prisma",
    "server": "ts-node-dev src/app/main",
    "dev": "concurrently --kill-others \"npm run server\"",
    "poststart": "npm run dev"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/jsonwebtoken": "^8.5.9",
    "prisma": "^4.8.0",
    "ts-node": "^10.9.1"
  },
  "dependencies": {
    "@prisma/client": "^4.8.0",
    "apollo-server": "^3.11.1",
    "apollo-server-express": "^3.11.1",
    "apollo-server-micro": "^3.11.1",
    "axios": "^1.2.0",
    "concurrently": "^7.6.0",
    "dotenv": "^16.0.3",
    "ethers": "^5.7.2",
    "express": "^4.18.2",
    "graphql": "^16.6.0",
    "jsonwebtoken": "^8.5.1",
    "micro-cors": "^0.1.1",
    "nexus": "^1.3.0",
    "npm-run-all": "^4.1.5",
    "run-s": "^0.0.0",
    "ts-node-dev": "^2.0.0",
    "typescript": "^4.8.4",
    "web3": "^1.8.1",
    "web3-utils": "^1.8.1"
  }
}
